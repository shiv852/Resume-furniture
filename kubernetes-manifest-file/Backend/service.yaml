apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: three-tier
spec:
  type: ClusterIP
  selector:
    role: api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
